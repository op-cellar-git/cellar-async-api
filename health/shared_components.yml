components:
  messageTraits:
    commonPayload:
      payload:
        type: object
        properties:
          SagaID:
            description: Machine-ID
            type: string
            maximum: 1
            minimum: 1
          ActivityID:
            description: Health
            type: string
            maximum: 1
            minimum: 1
